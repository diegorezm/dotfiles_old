#!/bin/bash 

# For arch only because im lazy
#


Configuration_arch(){
          sudo pacman -Syyu && sudo pacman -S --needed - < pkglist.txt
          mkdir -p ~/.config/ && mv .config/*  ~/.config/ 
          mkdir -p ~./local/ && mv .local/bin ~/.local/bin 
          mv .* ~/
  }

Configuration_arch
